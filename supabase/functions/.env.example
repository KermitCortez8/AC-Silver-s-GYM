# Configurar en Supabase > Edge Functions > Secrets.
# No copiar las variables VITE_ ni las credenciales de Stripe.
GMAIL_EMAIL=gimnasio@gmail.com
GMAIL_APP_PASSWORD=
EMAIL_FROM_NAME=Silver Gym Surco
FRONTEND_PUBLIC_URL=https://tu-aplicacion.com
# Generar con: python -c "import secrets; print(secrets.token_urlsafe(48))"
# Copiar el mismo valor en Vault, con el nombre silver_gym_schedule_cron_secret.
SCHEDULE_CRON_SECRET=
# SUPABASE_URL y SUPABASE_SERVICE_ROLE_KEY ya existen en la función alojada.
